const Title = ({title}) => {
    return (
        <div className="pt-12 pb-6">
            <h1 className="text-3xl font-bold dark:text-white text-center uppercase">{title}</h1>
        </div>
    );
};

export default Title;